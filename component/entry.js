/**
 * Bundle entry: the d3-hierarchy pieces used by treemap.js. Compiled to the
 * committed component/d3_hierarchy.js ESM bundle via `npm run bundle`.
 */

export { hierarchy, treemap, treemapSquarify } from 'd3-hierarchy';
